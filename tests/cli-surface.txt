# Every argument each subcommand accepts, one `command  argument` pair per line.
#
# The third CLI gate, and it exists because the other two are blind to this one thing.
# `cli()` registers all twenty-two subcommands from the COMMANDS table unconditionally; its
# `match c.name` arms only *add arguments*. So deleting an arm leaves the command present and
# strips its flags — invisible to `tests/dispatch.rs::every_command_dispatches`, which invokes
# each command with no arguments, and invisible to `command_table_matches_section_23`, which
# checks the table rather than the parser. Seven such mutants survived at 1.1 and this is what
# reaches them.
#
# It is compared inside `cargo test` rather than by a Makefile target on purpose. `make
# api-check` guards the library surface the same way, and nothing that measures coverage can
# see a Makefile — the reason `doc-output` was wired into the test suite in 0.13.
#
# Globals appear on every command, so dropping one shows up twenty-two times and dropping a
# command's own argument shows up once. Both are visible; neither is silent.
#
# Identical under `--no-default-features --features cli`, the default build, and
# `--all-features`: the arms are not feature-gated, so what a build *accepts* does not change
# with its features, only what it then does. Verified, not assumed.
#
# Regenerate with `make cli-surface` when a change here is deliberate.
fmt        [FILE]
fmt        --config
fmt        --check
fmt        --store
fmt        --write
fmt        --output
fmt        --format
fmt        --strict
fmt        --offline
fmt        --no-color
fmt        --noprogress
fmt        --json
fmt        --quiet
fmt        --verbose
fmt        --seed-check
fmt        --help
check      [FILE]
check      --config
check      --conformance
check      --as
check      --store
check      --granularity
check      --output
check      --format
check      --pass
check      --strict
check      --offline
check      --no-color
check      --noprogress
check      --json
check      --quiet
check      --verbose
check      --seed-check
check      --help
pack       [PATH]
pack       --budget
pack       --config
pack       --focus
pack       --output
pack       --query
pack       --format
pack       --query-limit
pack       --lod
pack       --strict
pack       --explain
pack       --offline
pack       --no-color
pack       --tokenizer
pack       --mode
pack       --noprogress
pack       --json
pack       --quiet
pack       --verbose
pack       --seed-check
pack       --help
merge      <STORE>
merge      --config
merge      --policy
merge      --retraction
merge      --store
merge      --output
merge      --staged
merge      --fail-on-contention
merge      --format
merge      --max-contentions-per-agent
merge      --strict
merge      --offline
merge      --no-color
merge      --noprogress
merge      --json
merge      --quiet
merge      --verbose
merge      --seed-check
merge      --help
diff       <STORE>
diff       --config
diff       --hop
diff       --by-agent
diff       --store
diff       --output
diff       --recipe
diff       --format
diff       --strict
diff       --offline
diff       --no-color
diff       --noprogress
diff       --json
diff       --quiet
diff       --verbose
diff       --seed-check
diff       --help
trace      <UID>
trace      [PATH]
trace      --config
trace      --depth
trace      --parents
trace      --grounds
trace      --output
trace      --both
trace      --format
trace      --agents
trace      --strict
trace      --offline
trace      --no-color
trace      --noprogress
trace      --json
trace      --quiet
trace      --verbose
trace      --seed-check
trace      --help
view       [PATH]
view       --config
view       --roots
view       --id
view       --output
view       --threads
view       --format
view       --requires
view       --strict
view       --offline
view       --no-color
view       --noprogress
view       --json
view       --quiet
view       --verbose
view       --seed-check
view       --help
bundle     [PATH]
bundle     --config
bundle     --view
bundle     --include-retracted
bundle     --output
bundle     --format
bundle     --strict
bundle     --offline
bundle     --no-color
bundle     --noprogress
bundle     --json
bundle     --quiet
bundle     --verbose
bundle     --seed-check
bundle     --help
thread     [PATH]
thread     --config
thread     --derive
thread     --schema
thread     --output
thread     --only
thread     --format
thread     --list
thread     --show
thread     --strict
thread     --id
thread     --offline
thread     --as
thread     --no-color
thread     --noprogress
thread     --scope
thread     --arity
thread     --json
thread     --explain
thread     --quiet
thread     --verbose
thread     --seed-check
thread     --help
salience   [PATH]
salience   --config
salience   --hop
salience   --top
salience   --explain
salience   --output
salience   --format
salience   --weights
salience   --seed
salience   --strict
salience   --offline
salience   --no-color
salience   --noprogress
salience   --json
salience   --quiet
salience   --verbose
salience   --seed-check
salience   --help
find       <QUERY>
find       [PATH]
find       --config
find       --limit
find       --kind
find       --min-status
find       --output
find       --format
find       --strict
find       --offline
find       --no-color
find       --noprogress
find       --json
find       --quiet
find       --verbose
find       --seed-check
find       --help
retract    <UID>
retract    [PATH]
retract    --config
retract    --dry-run
retract    --as
retract    --output
retract    --reason
retract    --authority
retract    --format
retract    --strict
retract    --offline
retract    --no-color
retract    --noprogress
retract    --json
retract    --quiet
retract    --verbose
retract    --seed-check
retract    --help
render     [PATH]
render     --config
render     --thread
render     --profile
render     --output
render     --target
render     --format
render     --lod
render     --contentions
render     --strict
render     --as
render     --offline
render     --no-color
render     --profiles
render     --noprogress
render     --json
render     --quiet
render     --verbose
render     --seed-check
render     --help
import     <PATH>
import     --config
import     --key
import     --kind
import     --store
import     --output
import     --format
import     --strict
import     --offline
import     --no-color
import     --noprogress
import     --json
import     --quiet
import     --verbose
import     --seed-check
import     --help
relink     [PATH]
relink     --config
relink     --dry-run
relink     --store
relink     --output
relink     --format
relink     --strict
relink     --offline
relink     --no-color
relink     --noprogress
relink     --json
relink     --quiet
relink     --verbose
relink     --seed-check
relink     --help
compact    [PATH]
compact    --config
compact    --dry-run
compact    --store
compact    --output
compact    --format
compact    --strict
compact    --offline
compact    --no-color
compact    --noprogress
compact    --json
compact    --quiet
compact    --verbose
compact    --seed-check
compact    --help
ingest     [FILE]
ingest     --config
ingest     --hop
ingest     --rung
ingest     --store
ingest     --granularity
ingest     --output
ingest     --format
ingest     --path
ingest     --repair
ingest     --strict
ingest     --offline
ingest     --yes
ingest     --dry-run
ingest     --no-color
ingest     --noprogress
ingest     --json
ingest     --quiet
ingest     --verbose
ingest     --seed-check
ingest     --help
attest     [PATH]
attest     --config
attest     --what
attest     --sample
attest     --output
attest     --format
attest     --strict
attest     --offline
attest     --no-color
attest     --noprogress
attest     --json
attest     --quiet
attest     --verbose
attest     --seed-check
attest     --help
providers  --config
providers  --probe
providers  --models
providers  --store
providers  --output
providers  --tasks
providers  --format
providers  --strict
providers  --offline
providers  --no-color
providers  --noprogress
providers  --json
providers  --quiet
providers  --verbose
providers  --seed-check
providers  --help
usage      --by
usage      --config
usage      --store
usage      --since
usage      --output
usage      --reset
usage      --format
usage      --strict
usage      --offline
usage      --no-color
usage      --noprogress
usage      --json
usage      --quiet
usage      --verbose
usage      --seed-check
usage      --help
reindex    [PATH]
reindex    --config
reindex    --verify
reindex    --output
reindex    --format
reindex    --strict
reindex    --offline
reindex    --no-color
reindex    --noprogress
reindex    --json
reindex    --quiet
reindex    --verbose
reindex    --seed-check
reindex    --help
ui         [PATH]
ui         --config
ui         --store
ui         --output
ui         --format
ui         --strict
ui         --offline
ui         --no-color
ui         --noprogress
ui         --json
ui         --quiet
ui         --verbose
ui         --seed-check
ui         --help
